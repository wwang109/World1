# AGENTS.md — historical

The two-agent (Claude ↔ Codex CLI) arrangement this file described ended with
the first-generation UI (removed in commit fff2ced). Its working docs live in
`docs/history/` (codex-ui-guide, codex-handoff, codex-game-brief). For the
current charter, boundaries, and doc map, read `CLAUDE.md` and `docs/INDEX.md`.
