# UI Screenshots

Committed reference captures for Claude and future handoffs.

- `loadout-portrait.png` — current portrait Prep hub on the `Loadout` tab
- `bag-portrait.png` — current portrait Prep hub on the `Bag` tab
- `wiki-portrait.png` — current portrait Prep hub on the `Wiki` tab
- `battle-portrait.png` — current portrait `BattleScene` frame
- `loadout.png` — Prep hub on the `Loadout` tab
- `bag.png` — Prep hub on the `Bag` tab
- `wiki.png` — Prep hub on the `Wiki` tab
- `battle.png` — mid-fight `BattleScene` frame

The `*-portrait.png` files are the active references. The older landscape PNGs
are retained only for comparison.

The same states can be reopened locally through the dev launcher:

- `/?view=loadout`
- `/?view=bag`
- `/?view=wiki`
- `/?scene=battle&enemy=bandit_duelist&seed=1`
